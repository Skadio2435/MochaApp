import { expect } from 'chai';
import num from './session.service';

describe('Session test', () => {
    it('and first', () => {
        expect(1).to.be.equal(1);
        expect(num).to.be.equal(22);
    });
});
